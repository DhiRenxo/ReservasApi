# Core FastAPI + ASGI server
fastapi==0.111.0
uvicorn[standard]==0.30.1
watchfiles==0.21.0  # Soporte para recarga automática

# ORM y base de datos
sqlalchemy==2.0.30
# psycopg2-binary==2.9.9  # Solo si usas PostgreSQL en producción

# Alembic para migraciones
alembic==1.13.1

# Validaciones y esquemas
pydantic==1.10.13
# Si usas Pydantic v2, deberías actualizar fastapi >=0.110 y adaptar validaciones

# Seguridad y autenticación JWT
passlib[bcrypt]==1.7.4
python-jose==3.3.0

# Autenticación con Google
httpx==0.27.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0

# Envío de correos con SendGrid
sendgrid==6.11.0

# Manejo de formularios y archivos
python-multipart==0.0.9

# Validación de correos y .env
email-validator==2.1.1
python-dotenv==1.0.1

# Utilidades generales
loguru==0.7.2
python-slugify==8.0.4
arrow==1.3.0
phonenumbers==8.13.37
# tzdata==2024.1  # Solo si trabajas en entornos sin zona horaria del sistema
